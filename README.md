# URL Shortener

Тестовое задание для стажировки в Positive Technologies: HTTP-сервис сокращения ссылок.

Планируемый API:

- `POST /shorten` — создать короткую ссылку;
- `GET /{code}` — перенаправить на исходный URL.

Рабочая ветка: `feature/url-shortener`.

